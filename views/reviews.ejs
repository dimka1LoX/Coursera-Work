<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://getbootstrap.com/docs/5.1/dist/css/bootstrap.min.css">
    <title>Отзывы</title>
</head>
<body>
    <%- include("partials/header") %>
    
    <div class="container mt-5">
        <h1 class="text-center mb-4">Чат</h1>

        <% if (user) { %>
            <form action="/reviews" method="POST" class="mb-4">
                <div class="mb-3">
                    <label for="review_text" class="form-label">Сообщение:</label>
                    <textarea class="form-control" name="review_text" required></textarea>
                </div>
                
                <button type="submit" class="btn btn-primary">Отправить</button>
            </form>
        <% } else { %>
            <div class="alert alert-warning" role="alert">
                Чтобы написать в чат, вам нужно <a href="/authorization" class="alert-link">войти в систему</a>.
            </div>
        <% } %>

        <h2 class="mb-3">Сообщения:</h2>
        <ul class="list-group">
            <% reviews.forEach(review => { %>
                <li class="list-group-item">
                    <strong><%= review.email %></strong>: <%= review.review_text %> <em>(<%= review.created_at.toLocaleString() %>)</em>
                </li>
            <% }); %>
        </ul>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js" defer></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.10.2/dist/umd/popper.min.js" defer></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/5.1.3/js/bootstrap.bundle.min.js" defer></script>
</body>
</html>